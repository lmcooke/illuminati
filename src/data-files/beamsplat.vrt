#version 410

in vec3 Position;
in vec3 Major;
in vec3 Minor;
in vec3 Power;

out vec3 major;
out vec3 minor;
out vec3 power_geo;

void main(void) {

        gl_Position = vec4(Position, 1.0);
        major = Major;
        minor = Minor;
        power_geo = Power;

}
